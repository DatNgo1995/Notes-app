import React from 'react'

export const editMode = ({title,content}) => {
    
    return (
        <div>
            <input type="text"></input>
            <textarea></textarea>
        </div>
    )
}